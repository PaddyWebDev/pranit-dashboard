          <TabsContent value="overview" className="mt-4 space-y-4">
            <div className="grid grid-cols-1 lg:grid-cols-3 gap-4">
              {/* Line chart – water usage */}
              <Card className="lg:col-span-2 border border-slate-100 dark:border-slate-800 shadow-sm">
                <CardHeader className="pb-2">
                  <CardTitle className="text-sm font-semibold text-slate-700 dark:text-slate-200 flex items-center gap-2">
                    <BarChart3 className="w-4 h-4 text-teal-500" />
                    प्रकल्पनिहाय पाणी वापर — Water Usage per Project (MCM)
                  </CardTitle>
                </CardHeader>
                <CardContent>
                  {loading ? (
                    <Skeleton className="h-64 w-full" />
                  ) : (
                    <ResponsiveContainer width="100%" height={260}>
                      <AreaChart
                        data={recentWater}
                        margin={{ top: 5, right: 10, left: 0, bottom: 5 }}
                      >
                        <defs>
                          {[
                            { id: "tembhu", color: COLORS.tembhu },
                            { id: "mhaisal", color: COLORS.mhaisal },
                            { id: "takari", color: COLORS.takari },
                          ].map(({ id, color }) => (
                            <linearGradient
                              key={id}
                              id={`grad-${id}`}
                              x1="0"
                              y1="0"
                              x2="0"
                              y2="1"
                            >
                              <stop
                                offset="5%"
                                stopColor={color}
                                stopOpacity={0.18}
                              />
                              <stop
                                offset="95%"
                                stopColor={color}
                                stopOpacity={0}
                              />
                            </linearGradient>
                          ))}
                        </defs>
                        <CartesianGrid strokeDasharray="3 3" stroke="#e2e8f0" />
                        <XAxis dataKey="year" tick={{ fontSize: 10 }} />
                        <YAxis tick={{ fontSize: 10 }} width={32} />
                        <Tooltip content={<CustomTooltip />} />
                        <Legend
                          iconType="circle"
                          iconSize={8}
                          wrapperStyle={{ fontSize: 11 }}
                        />
                        <Area
                          type="monotone"
                          dataKey="Tembhu"
                          stroke={COLORS.tembhu}
                          strokeWidth={2}
                          fill="url(#grad-tembhu)"
                          dot={false}
                        />
                        <Area
                          type="monotone"
                          dataKey="Mhaisal"
                          stroke={COLORS.mhaisal}
                          strokeWidth={2}
                          fill="url(#grad-mhaisal)"
                          dot={false}
                        />
                        <Area
                          type="monotone"
                          dataKey="Takari"
                          stroke={COLORS.takari}
                          strokeWidth={2}
                          fill="url(#grad-takari)"
                          dot={false}
                        />
                      </AreaChart>
                    </ResponsiveContainer>
                  )}
                </CardContent>
              </Card>

              {/* Pie chart – CCA */}
              <Card className="border border-slate-100 dark:border-slate-800 shadow-sm">
                <CardHeader className="pb-2">
                  <CardTitle className="text-sm font-semibold text-slate-700 dark:text-slate-200">
                    सिंचन श्रेत्र — CCA in Ha
                  </CardTitle>
                </CardHeader>
                <CardContent>
                  <ResponsiveContainer width="100%" height={200}>
                    <PieChart>
                      <Pie
                        data={CCA_PIE}
                        cx="50%"
                        cy="50%"
                        innerRadius={50}
                        outerRadius={80}
                        paddingAngle={2}
                        dataKey="value"
                      >
                        {CCA_PIE.map((entry, i) => (
                          <Cell key={i} fill={entry.color} />
                        ))}
                      </Pie>
                      <Tooltip
                        formatter={(v: any) => [`${v}%`, ""]}
                        contentStyle={{ fontSize: 12, borderRadius: 8 }}
                      />
                    </PieChart>
                  </ResponsiveContainer>
                  <div className="grid grid-cols-2 gap-x-4 gap-y-1 mt-2">
                    {CCA_PIE.map((item) => (
                      <div
                        key={item.name}
                        className="flex items-center gap-1.5 text-xs text-slate-600 dark:text-slate-300"
                      >
                        <span
                          className="w-2.5 h-2.5 rounded-sm flex-shrink-0"
                          style={{ background: item.color }}
                        />
                        <span>{item.name}</span>
                        <span className="ml-auto font-medium">
                          {item.value}%
                        </span>
                      </div>
                    ))}
                  </div>
                </CardContent>
              </Card>
            </div>

            {/* Total water trend full */}
            <Card className="border border-slate-100 dark:border-slate-800 shadow-sm">
              <CardHeader className="pb-2">
                <CardTitle className="text-sm font-semibold text-slate-700 dark:text-slate-200 flex items-center gap-2">
                  <TrendingUp className="w-4 h-4 text-violet-500" />
                  Annual Total Water Usage — All Years (2002–2025)
                </CardTitle>
              </CardHeader>
              <CardContent>
                {loading ? (
                  <Skeleton className="h-48 w-full" />
                ) : (
                  <ResponsiveContainer width="100%" height={200}>
                    <BarChart
                      data={waterData}
                      margin={{ top: 5, right: 10, left: 0, bottom: 20 }}
                    >
                      <CartesianGrid strokeDasharray="3 3" stroke="#e2e8f0" />
                      <XAxis
                        dataKey="year"
                        tick={{ fontSize: 9 }}
                        angle={-45}
                        textAnchor="end"
                        interval={2}
                      />
                      <YAxis tick={{ fontSize: 10 }} width={32} />
                      <Tooltip content={<CustomTooltip />} />
                      <Bar
                        dataKey="Total"
                        fill={COLORS.total}
                        radius={[3, 3, 0, 0]}
                      />
                    </BarChart>
                  </ResponsiveContainer>
                )}
              </CardContent>
            </Card>
          </TabsContent>
